
@using EntityLayer.Concrete
@model List<Blog>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<!DOCTYPE html>
<html>
<body>
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-3">
                    <div class="ibox">
                        <div class="ibox-content product-box">
                            <img src="@item.BlogImage" style="height:100%;width:100%;" />
                            <div class="product-desc">
                                <span class="product-price">
                                    @(((DateTime)item.BlogCreatedDate).ToString("dd-MMM-yyyy"))
                                </span>
                                <small class="text-muted">@item.Category.CategoryName</small>
                                <a href="#" class="product-name"> @item.BlogTitle</a>
                                <div class="small m-t-xs">
                                    @item.BlogContent.Substring(0, item.BlogContent.Substring(0, 100).LastIndexOf(" "))...
                                </div>
                                <div class="m-t text-righ">
                                    <a href="/Blog/BlogReadAll/@item.BlogID" class="btn btn-xs btn-outline btn-primary">Details <i class="fa fa-long-arrow-right"></i> </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</body>
</html>